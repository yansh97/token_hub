[package]
name = "token_proxy_protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
base64 = "0.22"
bytes = "1"
http = "1"
serde_json = "1.0.151"
tiktoken-rs = "0.12"
tracing = { version = "0.1.44", features = ["release_max_level_off"] }
