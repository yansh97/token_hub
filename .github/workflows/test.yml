name: Test

on:
  push:
    branches:
      - main
      - release/**
  workflow_dispatch:
  workflow_call:

env:
  CARGO_TERM_COLOR: always
  NODE_VERSION: 24
  PNPM_VERSION: 10.13.1

jobs:
  test:
    name: Test (Rust)
    # 单人维护时，普通 push 依赖本地验证；正式 release 提交仍由 CI 完整验证。
    if: >-
      ${{ github.event_name != 'push' || startsWith(github.event.head_commit.message, 'chore: token-hub release v') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Ensure pnpm store directory exists
        run: node scripts/ensure-pnpm-store.mjs

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Build frontend
        run: pnpm build

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "src-tauri"
          cache-on-failure: true
          # Keep rustup/cargo binaries from the runner; cached ~/.cargo/bin can shadow cargo.
          cache-bin: "false"
          prefix-key: "v1-rust"

      - name: Run cargo test
        working-directory: src-tauri
        run: cargo test
